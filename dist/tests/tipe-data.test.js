"use strict";
describe('Data Type', () => {
    it('Should must declare', () => {
        let name = 'Muhammad Fatih Al Fawwaz';
        let balance = 1000000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // Gunakan npx tsc --watch untuk mengecek kode perubahan secara terus menerus
        // name = 100;
        // balance = 'string';
        // isVip = 100
    });
});
