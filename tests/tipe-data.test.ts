describe('Data Type', () => {
  it('Should must declare', () => {
    let name: string = 'Muhammad Fatih Al Fawwaz';
    let balance: number = 1000000;
    let isVip: boolean = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);

    // Gunakan npx tsc --watch untuk mengecek kode perubahan secara terus menerus
    // name = 100;
    // balance = 'string';
    // isVip = 100
  });
});
